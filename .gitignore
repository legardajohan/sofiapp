# Dependencias
node_modules/
.pnpm-store/

# Builds
dist/
build/
.turbo/
*.tsbuildinfo

# Entorno y secretos
.env
.env.*
!.env.example
*.pem
*.key

# Tests / cobertura
coverage/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editor / SO
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Claude Code — configuración local personal (no compartida)
.claude/settings.local.json
.mcp.json

# Material de trabajo desactualizado (no es fuente de verdad)
dev/

# Binario MongoDB para tests (se descarga automáticamente en el primer test run)
.mongodb-binaries/

# Snapshots temporales de Playwright MCP
.playwright-mcp/

# Capturas/imágenes sueltas (no deben commitearse por error)
*.png

# MCP de Open Code
opencode.json

# Scripts de debug ad-hoc (pueden contener credenciales sueltas, nunca son fuente de verdad)
apps/backend/check-*.cjs